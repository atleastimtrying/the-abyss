<% # render '/users/login' %>
<div data-id="<%= @plot.id %>" data-x="<%= @plot.x %>" data-y="<%= @plot.y %>" class='actions'>
  <h2><%= @plot.title %></h2>
  <section>
    <p><%= @plot.description %></p>
  </section>
</div>
<nav>
  <a href='/north' class="north">North</a>
  <a href='/south' class="south">South</a>
  <a href='/east' class="east">East</a>
  <a href='/west' class="west">West</a>
</nav>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
<%= render '/devise/sessions/new', :resource => User.new() , :resource_name => :user %>
